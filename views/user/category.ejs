<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Event IO</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link rel="stylesheet" href="/stylesheets/category.css">
</head>
<body>
    <header>
        <div class="logo">
            Event <span class="logo-2">IO</span>
        </div>
    </header>
    <main>
        <section class="category-header">
            <a href="/" class="back-button">Back to Home</a>
            <h1>People who specialize in <%= skill %></h1>
        </section>
        <section class="user-list">
            <% for(let user of user_info){ %>
                <div class="user-card">
                    <h2><%= user.f_name %></h2>
                    <p>Email: <%= user.email %></p>
                    <p><span style="font-weight: bold;">Skills: </span><%= user.skills.join(' | ') %></p>
                </div>
            <% } %>
        </section>
    </main>
</body>
</html>
